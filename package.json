{
  "name": "pallas",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter 'docs' run dev",
    "build": "pnpm recursive run build",
    "clean": "pnpm recursive run clean",
    "test": "pnpm recursive run test",
    "export": "pnpm recursive run export"
  },
  "devDependencies": {
    "concurrently": "^8.0.1",
    "rimraf": "^3.0.2",
    "run-pty": "4.0.4",
    "typescript": "5.1.6"
  },
  "engines": {
    "node": "18.x",
    "pnpm": "8.x"
  }
}
